import Table from "../admin/table";

export default function NoticeTable({ noticeData }) {
    const notice = noticeData;

    return (
        <Table>
            {
                
            }

        </Table>
    );
}